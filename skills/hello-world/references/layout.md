# Portable layout

`hello-world` follows the catalog contract:

- `SKILL.md` is the only required file.
- `references/` holds extra documentation loaded only when needed.
- `scripts/` holds optional helpers. Agents may run them; the hub CLI never does during install.
